# local artifacts
.DS_Store
*.env
*.local
mise.local.toml
examples/*-raw*.json

# Raw HAR captures contain live tokens and PII — keep local only.
# Redacted, shareable evidence belongs in examples/.
captures/

# Built CLI binary (mise builds to bin/, which is on PATH; a plain
# `go build` in cli/ produces cli/skylight)
/bin/skylight
cli/skylight
