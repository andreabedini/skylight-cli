// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/skylight/skylight-cli/runtime"
	"github.com/spf13/cobra"
)

var rewardsDeleteCmd = &cobra.Command{
	Use:   "delete",
	Short: "Delete a reward",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/api/frames/{frameId}/rewards/{rewardId}"
		frameIdVal, _ := cmd.Flags().GetString("frame-id")
		if frameIdVal == "" {
			return fmt.Errorf("required flag --%s not set", "frame-id")
		}
		path = strings.Replace(path, "{frameId}", frameIdVal, 1)
		rewardIdVal, _ := cmd.Flags().GetString("reward-id")
		if rewardIdVal == "" {
			return fmt.Errorf("required flag --%s not set", "reward-id")
		}
		path = strings.Replace(path, "{rewardId}", rewardIdVal, 1)

		params := make(map[string]string)

		headers := make(map[string]string)
		resp, err := client.Do("DELETE", path, params, headers, nil)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	rewardsDeleteCmd.Flags().String("frame-id", "", "")
	_ = rewardsDeleteCmd.MarkFlagRequired("frame-id")
	rewardsDeleteCmd.Flags().String("reward-id", "", "")
	_ = rewardsDeleteCmd.MarkFlagRequired("reward-id")
}
