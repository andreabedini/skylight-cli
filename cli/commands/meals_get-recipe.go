// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/skylight/skylight-cli/runtime"
	"github.com/spf13/cobra"
)

var mealsGetRecipeCmd = &cobra.Command{
	Use:   "get-recipe",
	Short: "Get a meal recipe by id",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/api/frames/{frameId}/meals/recipes/{recipeId}"
		frameIdVal, _ := cmd.Flags().GetString("frame-id")
		if frameIdVal == "" {
			return fmt.Errorf("required flag --%s not set", "frame-id")
		}
		path = strings.Replace(path, "{frameId}", frameIdVal, 1)
		recipeIdVal, _ := cmd.Flags().GetString("recipe-id")
		if recipeIdVal == "" {
			return fmt.Errorf("required flag --%s not set", "recipe-id")
		}
		path = strings.Replace(path, "{recipeId}", recipeIdVal, 1)

		params := make(map[string]string)

		headers := make(map[string]string)
		resp, err := client.Do("GET", path, params, headers, nil)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	mealsGetRecipeCmd.Flags().String("frame-id", "", "")
	_ = mealsGetRecipeCmd.MarkFlagRequired("frame-id")
	mealsGetRecipeCmd.Flags().String("recipe-id", "", "")
	_ = mealsGetRecipeCmd.MarkFlagRequired("recipe-id")
}
